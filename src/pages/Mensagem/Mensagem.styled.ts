import { styled } from '@mui/material';
import Button from '@mui/material/Button';

export const BotaoVoltar = styled(Button)({
  backgroundColor: '#CFA730',
  color: 'white',
  fontWeight: 'bold',
  '&:hover': {
    backgroundColor: '#7D651D',
  },
});
